{
  "name": "verdaccio-0",
  "version": "1.0.6",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf ./dist && tsc",
    "dev": "tsx --watch ./src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "admin",
  "license": "ISC",
  "description": "这是一个简单的测试，为了验证verdaccio是否可用",
  "devDependencies": {
    "rimraf": "6.0.1",
    "tsx": "4.19.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "dayjs": "1.11.13"
  }
}
